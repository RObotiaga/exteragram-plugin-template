"""Additional Python modules bundled into the EAF as the ``src`` package.

Put reusable Python code in this directory and import it from the plugin entry
point with imports such as ``from src.my_module import helper``.
"""
